export const baseUrl = 'http://127.0.0.1:3000';

export enum path {
  users = '/users',
  categories = '/category',
  lessons = '/lessons',
  tests = '/tests',
  tasks = '/tasks',
  comments = '/comments',
}
