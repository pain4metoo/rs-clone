export const baseUrl = 'http://localhost:3000'; //http://127.0.0.1:3000';

export enum path {
  users = '/users',
  register = '/register',
  login = '/login',
  categories = '/category',
  lessons = '/lessons',
  tests = '/tests',
  tasks = '/tasks',
  comments = '/comments',
  settings = '/settings',
}
